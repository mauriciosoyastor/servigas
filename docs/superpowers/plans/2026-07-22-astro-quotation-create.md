# Plan — Create cotización Astro

## Tasks

- [x] TDD `order-creates` filter (partnerId, productId, qty)
- [x] TDD adapter `createRecord` → sale.order + line
- [x] UI `/lists/sales/quotations/new` + CTA lista
- [x] Suite + smoke create → confirm

## Smoke

- Create id `3` → confirm → `state: sale`
