[options]
; Config Odoo para el stack Docker del mostrador (opción C).
; Credenciales DB vienen del entrypoint oficial (HOST/USER/PASSWORD).
addons_path = /mnt/extra-addons
data_dir = /var/lib/odoo
admin_passwd = servigas_master_change_me
list_db = True
dbfilter = ^servigas$
http_port = 8069
proxy_mode = False
