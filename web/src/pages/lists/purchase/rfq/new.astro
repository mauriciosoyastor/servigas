---
import OrderCreateForm from '../../../../components/OrderCreateForm.astro';
import ShellLayout from '../../../../layouts/ShellLayout.astro';
import { invalidateBffSession, requireOdooSession } from '../../../../lib/bff/http.ts';

let userName = '';

try {
	const { session } = requireOdooSession(Astro.cookies);
	userName = session.name || session.login;
} catch {
	invalidateBffSession(Astro.cookies);
	return Astro.redirect('/login');
}
---

<ShellLayout title="Nueva RFQ | Servigas" active="purchase" userName={userName}>
	<main class="sg-create-page">
		<header>
			<p>Compras</p>
			<h1>Nueva RFQ</h1>
		</header>
		<OrderCreateForm
			apiPath="/api/records/purchase/rfq"
			listPath="/lists/purchase/rfq"
			partnerListKey="purchase/vendors"
			productListKey="inventory/variants"
			title="Alta de solicitud de compra"
			hint="Buscá proveedor y producto por nombre o código. Queda en borrador para confirmar después."
			partnerLabel="Proveedor"
			partnerPlaceholder="Buscar proveedor…"
		/>
	</main>
</ShellLayout>

<style>
	.sg-create-page {
		max-width: 40rem;
		margin: 0 auto;
		display: grid;
		gap: 1.25rem;
	}
	.sg-create-page header p {
		margin: 0 0 0.35rem;
		color: var(--sg-text-muted-dark);
		font-size: 0.78rem;
		font-weight: 500;
	}
	.sg-create-page header h1 {
		margin: 0;
		font-size: clamp(1.8rem, 3.5vw, 2.4rem);
		font-weight: 600;
		letter-spacing: -0.04em;
	}
</style>
