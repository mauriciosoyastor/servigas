<aside class="sg-coming-soon sg-glass" data-coming-soon hidden aria-live="polite">
	<span class="sg-coming-soon-dot" aria-hidden="true"></span>
	<div>
		<strong>Próximamente</strong>
		<p data-coming-soon-detail>
			Este acceso todavía no tiene pantalla Astro en el camino a corte.
		</p>
	</div>
	<button class="sg-focus-ring" type="button" data-coming-soon-close aria-label="Cerrar aviso">×</button>
</aside>

<style>
	.sg-coming-soon {
		position: fixed;
		z-index: 40;
		right: 1.5rem;
		bottom: 1.5rem;
		display: grid;
		max-width: min(24rem, calc(100vw - 2rem));
		grid-template-columns: auto 1fr auto;
		align-items: start;
		gap: 0.8rem;
		border-radius: 1.15rem;
		padding: 1rem;
		color: white;
		box-shadow: var(--sg-shadow-lg);
	}
	.sg-coming-soon[hidden] {
		display: none;
	}
	.sg-coming-soon-dot {
		width: 0.65rem;
		height: 0.65rem;
		margin-top: 0.3rem;
		border-radius: 50%;
		background: var(--sg-flame-gradient);
		box-shadow: 0 0 16px var(--sg-flame-orange);
	}
	.sg-coming-soon strong {
		font-size: 0.86rem;
	}
	.sg-coming-soon p {
		margin: 0.3rem 0 0;
		color: var(--sg-text-muted-dark);
		font-size: 0.78rem;
		line-height: 1.45;
	}
	.sg-coming-soon button {
		border: 0;
		color: var(--sg-text-muted-dark);
		background: transparent;
		cursor: pointer;
		font-size: 1.25rem;
		line-height: 1;
	}
	@media (max-width: 720px) {
		.sg-coming-soon {
			right: 1rem;
			bottom: 6.5rem;
		}
	}
</style>
